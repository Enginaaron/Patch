{
  "name": "patch-backend",
  "private": true,
  "version": "0.0.0",
  "description": "Patch FastAPI backend",
  "scripts": {
    "dev": "python -m uvicorn app.main:app --reload",
    "start": "python -m uvicorn app.main:app",
    "install:python": "python -m pip install -r requirements.txt"
  }
}
